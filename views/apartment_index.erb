<div class="d-flex justify-content-between align-items-center mb-4">
  <h1>Apartments</h1>
</div>

<div class="row">
  <% if @apartments.empty? %>
    <div class="col-12 text-center py-5">
      <p class="lead text-muted">No apartments found.</p>
    </div>
  <% else %>
    <% @apartments.each do |apartment| %>
      <div class="col-md-4 mb-4">
        <div class="card h-100 apartment-card">
          <img src="https://images.unsplash.com/photo-1493809842364-78817add7ffb?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=2070&q=80" class="card-img-top" style="height: 200px; object-fit: cover;">
          
          <div class="card-body">
            <h5 class="card-title"><%= h apartment[:title] %></h5>
            <p class="card-text text-muted mb-2"><%= h apartment[:city] %>, <%= h apartment[:state] %></p>
            
            <div class="mb-2">
              <span class="badge bg-primary"><%= apartment[:bedrooms] %> <%= apartment[:bedrooms] == 1 ? 'Bedroom' : 'Bedrooms' %></span>
              <span class="badge bg-info text-dark"><%= apartment[:bathrooms] %> <%= apartment[:bathrooms] == 1 ? 'Bathroom' : 'Bathrooms' %></span>
              <% if apartment[:square_feet] %>
                <span class="badge bg-secondary"><%= apartment[:square_feet] %> sq ft</span>
              <% end %>
            </div>
            
            <p class="card-text"><%= h apartment[:description][0..100] %>...</p>
            
            <div class="d-flex justify-content-between align-items-center">
              <h5 class="text-success mb-0">$<%= number_with_precision(apartment[:price]) %></h5>
              <a href="/apartments/<%= apartment[:id] %>" class="btn btn-sm btn-outline-primary">View Details</a>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  <% end %>
</div>